{
    "name": "mi-aplicacion-web",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "install": "cd backend && npm install && cd ../frontend && npm install",
      "build": "cd frontend && npm run build",
      "deploy": "echo 'Deploying the app...'"
    },
    "devDependencies": {
      "concurrently": "^9.1.2"
    }
  }
  